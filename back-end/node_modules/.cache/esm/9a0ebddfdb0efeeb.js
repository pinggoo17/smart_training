let Joi,Category;_db0‍.x([["list",()=>list],["add",()=>add],["remove",()=>remove]]);_db0‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_db0‍.w("../../models/category",[["default",["Category"],function(v){Category=v}]]);


       const list = async (ctx) => {
  const schema = Joi.object().keys({
    username: Joi.string().required(),
  });

  const result = schema.validate(ctx.request.body);

  if (result.error) {
    ctx.status = 400;
    ctx.body = result.error;
  }

  const { username } = ctx.request.body;

  try {
    const exists = await Category.findByUsername(username);

    if (!exists) {
      ctx.body = "none data";
      return;
    }

    ctx.body = exists;
  } catch (e) {
    ctx.throw(500, e);
  }
};

       const add = async (ctx) => {
  const schema = Joi.object().keys({
    username: Joi.string().required(),
    title: Joi.string().required(),
  });

  const result = schema.validate(ctx.request.body);

  if (result.error) {
    ctx.status = 400;
    ctx.body = result.error;
  }

  const { username, title } = ctx.request.body;

  const category = new Category({
    username,
    title,
  });

  try {
    await category.save();
    ctx.body = category;
  } catch (e) {
    ctx.throw(500, e);
  }
};

       const remove = async (ctx) => {
  const { id } = ctx.request.body;

  try {
    await Category.findByIdAndRemove(id).exec();
    ctx.status = 204;
  } catch (e) {
    ctx.throw(500, e);
  }
};
